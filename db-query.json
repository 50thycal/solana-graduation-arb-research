{
  "queries": [
    { "id": "verify-count", "sql": "SELECT COUNT(*) AS n FROM graduation_momentum", "max_rows": 10 },
    { "id": "verify-latest", "sql": "SELECT graduation_id, mint, label, pct_t300 FROM graduation_momentum ORDER BY graduation_id DESC LIMIT 5", "max_rows": 10 },
    { "id": "verify-pragma", "sql": "PRAGMA table_info(graduation_momentum)", "max_rows": 200 },
    { "id": "verify-guard-rejects-write", "sql": "UPDATE graduation_momentum SET label='PUMP' WHERE 1=0", "max_rows": 10 }
  ]
}
