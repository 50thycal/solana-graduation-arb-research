{
  "queries": [
    { "id": "clean-grad-count", "sql": "SELECT COUNT(*) AS n FROM graduation_momentum", "max_rows": 10 },
    { "id": "clean-label-dist", "sql": "SELECT label, COUNT(*) AS n FROM graduation_momentum GROUP BY label ORDER BY n DESC", "max_rows": 20 },
    { "id": "clean-pragma", "sql": "PRAGMA table_info(graduation_momentum)", "max_rows": 250 },
    { "id": "clean-guard-write", "sql": "DELETE FROM graduation_momentum WHERE 1=0", "max_rows": 10 }
  ]
}
